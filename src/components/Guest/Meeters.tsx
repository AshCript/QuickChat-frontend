import React from 'react';
import { User } from './Users/User';
import './Users.css';

export const Meeters = ({ me, users }) => {
  // return (
  // <div className="users">
  //   All users :<br />
  //   {users.map((user: any) => {
  //     return user.email !== me.email ? (
  //       <User user={user} key={'user' + user.id} />
  //     ) : (
  //       ''
  //     );
  //   })}
  // </div>
  // );
};
